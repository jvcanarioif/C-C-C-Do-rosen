#include <stdio.h>

int main()
{
    int valor1, valor2;
    int desconto;


    printf("Informe qual o valor do produto: ");
    scanf("%d", &valor1);

    printf("Informe o desconto do produto: ");
    scanf("%d", &desconto);

    desconto = valor1 * desconto / 100;
    valor2 = valor1 - desconto

    printf("Desconto dro produlto é: %d\n", valor2);

    return 0;
}
