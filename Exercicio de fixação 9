#include <stdio.h>

int main()
{
    int km, dias, preco;
    printf("Escreva por quantos dias o carro ira ser alugado :");
    scanf("%d", &dias);
    printf("digite quantos quilometros o carro andou :");
    scanf("%d", &km);
    preco = (dias * 60) + (km * .15);
    printf("o valor total do aluguel é de R$%d", preco);
}
